import 'package:flutter/material.dart';

class MyProfilePageAdmin extends StatelessWidget {
  const MyProfilePageAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
